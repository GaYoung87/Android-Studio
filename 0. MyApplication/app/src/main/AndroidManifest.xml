<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <!-- activity : os에게 어플리케이션 구성, 정보 알려주는 용도-->
        <!-- 화면이 2개 이상 -> activity 2개 이상! -->
        <!-- activity의 name은 class의 이름  -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <!-- intent-filter : os가 각각의 구성요소를 구분하기 위한 것 -->
                <action android:name="android.intent.action.MAIN" />
                <!-- action : 외부에서 찾을 수 있는 이름 -->
                <!-- app에 activity 3개 만들어놨으면 3개화면 -> 그 중 어떤 화면이 첫화면? -> action name에 지정해준다 -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>